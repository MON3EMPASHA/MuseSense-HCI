\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{float}
\usepackage{enumitem}
\usepackage{changepage}
\usepackage{listings}
\usepackage{url}
\usepackage{booktabs}
\usepackage{array}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\lstset{
    language=Python,
    basicstyle=\ttfamily\footnotesize,
    showstringspaces=false,
    breaklines=true,
    frame=single,
    captionpos=b,
    columns=fullflexible,
    float=htbp,
}

\begin{document}

\title{MuseSense: A Multimodal Tangible Museum Guide for 3D Artifacts, Narration, and Vision-Based Personalization}

% \author{\IEEEauthorblockN{MuseSense HCI Project Team}}
%commented author for blind review
\maketitle

\begin{abstract}
MuseSense is a museum guide system that combines tangible markers, computer vision, interactive three-dimensional artifact models, and synchronized audio narration into a single interactive experience. The system uses reacTIVision/TUIO markers for direct artifact selection, a Python vision service for face-based session recognition and background sensing, and a C\# WinForms client for real-time 3D model rendering, narration playback, favorites management, and visitor-facing interaction. The implementation is split across two coordinated projects: the main museum application and a supporting background service for gesture and Bluetooth-based identity handling. This paper documents the design rationale, software architecture, interaction flow, and system-level validation of the current build, and includes a small comparative user study against a tablet baseline. It also situates MuseSense within the literature on embodied interaction, tangible interfaces, public displays, museum HCI, vision-based perception, and gesture recognition.
\end{abstract}

\begin{IEEEkeywords}
museum HCI, tangible interaction, TUIO, reacTIVision, MediaPipe, face recognition, 3D rendering, narration, embodied interaction
\end{IEEEkeywords}

\section{Introduction}
Museums increasingly rely on digital systems to enrich artifact exploration, but many installations still force visitors to choose between expressive physical interaction and structured screen-based navigation. MuseSense addresses this gap by turning museum browsing into a multimodal experience: visitors place tangible markers on an interactive surface to open artifacts, the system identifies users through vision-based login, and the desktop client presents 3D models, metadata, and narration audio in a responsive guided interface.

The current project is not a generic demo shell. It is a working integrated system with a full implementation stack: Python for camera capture, face recognition, and socket communication; C\# for desktop rendering, TUIO marker handling, and audio playback; and a separate gesture-oriented service that preserves the original gesture-research direction of the project. The paper has two goals. First, it documents the actual system that was built. Second, it explains how the implementation fits established HCI research on tangible interaction, embodied interfaces, public displays, and museum narration.

This paper makes four contributions. First, it presents an integrated museum system that combines marker-based tangible interaction, face-based personalization, three-dimensional artifact presentation, and narration playback in a unified visitor workflow. Second, it documents a distributed software architecture that separates perception and rendering concerns while preserving interaction continuity through explicit state synchronization. Third, it formalizes a state-driven interaction model that governs artifact selection, session binding, and favorites persistence in a shared public environment. Fourth, it provides a literature-grounded argument for why this embodied interaction strategy is appropriate for cultural heritage settings where social visibility, interpretive context, and ease of entry are central design constraints.

\section{Literature Review}
\subsection{Embodied and Tangible Interaction}
MuseSense builds on the classic argument that interaction should be understood through the body, physical space, and the meaning of artifacts rather than only through command syntax. Ishii and Ullmer's Tangible Bits vision framed physical objects as interfaces to digital information, showing how graspable artifacts can link the physical and virtual worlds in a more legible way than conventional windows-and-icons interfaces~\cite{ishii1997}. Dourish extended this direction by arguing that embodied interaction is not merely a richer input method, but a shift in how meaning is produced through situated action, physical engagement, and social context~\cite{dourish2001}.

Hornecker and Buur operationalized these ideas for tangible interaction by showing that TUI design must account for both physical manipulation and social coordination in shared spaces~\cite{horneckerbuur2006}. MuseSense follows this logic closely: the table marker is not only a selector, but also a social cue that invites visitors to manipulate a real object in front of others. That visible, embodied act is part of the experience rather than a side effect.

\subsection{Public Displays and Museums}
Museums are public settings, so the system must work not only as a technical interface but also as a socially understandable one. Brignull and Rogers showed that public displays attract attention when the interaction is visible and easy to interpret, while Muller et al. argued that public interfaces should be designed around entry, engagement, and return-to-passive states rather than assuming a single uninterrupted user~\cite{brignull2003,muller2010}. Reeves et al. further emphasized the spectator experience, noting that people who are not directly interacting still shape the meaning and social comfort of the interaction~\cite{reeves2005}.

Museums specifically have a long tradition of tangible and embodied interaction research. Hornecker and Stifter observed that interactive museum installations benefit from embodied facilitation, where the physical configuration of the exhibit helps visitors discover what to do without reading long instructions~\cite{horneckerstifter2006}. More recently, museum-HCI work has emphasized accessibility, narrative presentation, and multisensory interpretation as part of a richer visitor experience~\cite{pietroni2021}. MuseSense aligns with that direction by pairing artifact browsing with narration audio and personal favorites.

\subsection{Computer Vision and Gesture Recognition}
MuseSense uses vision not as a novelty, but as an infrastructure layer. MediaPipe provides a lightweight pipeline model for perception tasks that need to run in real time on commodity hardware~\cite{lugaresi2019}. BlazePose extends that idea with on-device real-time body pose tracking, making pose-based interaction feasible in interactive demos and mobile settings~\cite{bazarevsky2020}. In parallel, face recognition in MuseSense relies on a standard embedding-based approach. FaceNet demonstrated that face images can be mapped to a compact Euclidean embedding where distance corresponds to identity similarity, which fits the local identity model used in this project~\cite{schroff2015}. The Python backend also uses a face detector, which is consistent with the cascaded face-detection family introduced by Zhang et al.~\cite{zhang2016}.

The gesture side of the project is informed by recognizers that avoid heavy training requirements. Wobbrock et al. showed that a simple template recognizer can be effective for prototype interfaces~\cite{wobbrock2007}, Anthony and Wobbrock extended this direction to multistroke gestures~\cite{anthony2010}, and Vatavu et al. proposed the \$P recognizer for point-cloud gesture comparison~\cite{vatavu2012}. MuseSense uses that family of ideas in the background gesture service that supports motion-driven interaction experiments.

Social acceptability matters as much as recognition accuracy. Rico and Brewster showed that gestures can feel more or less appropriate depending on context and visibility~\cite{rico2010}, and Hoff and Bashir argued that repeated failures degrade trust in automation even when the system later recovers~\cite{hoff2015}. Those findings are directly relevant to a museum guide, because visitors will abandon the interface quickly if the system feels unstable or unpredictable.

\subsection{Narration and Evaluation}
Narration is not an accessory in MuseSense; it is part of the interpretive layer that turns an artifact into a story. In museum HCI, audio and narrative are often used to add context without crowding the visual interface, and evaluation is usually framed around workload, clarity, and visitor engagement rather than raw task speed~\cite{pietroni2021}. The present paper focuses on architecture and scenario-based validation, while keeping formal user-study methods such as NASA-TLX and thematic analysis available for future evaluation designs~\cite{hart1988,braun2006}.

\section{Methods}
\subsection{HCI Overview Diagram and Architecture}
MuseSense is implemented as a distributed local system rather than a monolithic desktop application. The end-to-end human-computer interaction flow is summarized in the full-width overview diagram in Fig.~\ref{fig:overview}. The diagram is intentionally organized around interaction semantics rather than code structure, so that perception, interpretation, interface response, and content delivery can be read as one continuous visitor journey.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{paper/system_architecture.jpeg}
\caption{MuseSense HCI overview diagram. The overview presents the complete interaction loop across sensing, state interpretation, tangible input handling, visual rendering, narration delivery, and preference persistence.}
\label{fig:overview}
\end{figure*}

At the perception boundary, camera and marker streams capture the visitor's embodied actions: face presence and identity cues support session binding, while marker identity and pose support direct artifact selection and manipulation. These signals are transformed into compact interaction-state events that are shared with the presentation client. This event-oriented exchange separates sensing latency from interface responsiveness, which is critical in public settings where short response delays can quickly degrade user confidence.

At the presentation boundary, the desktop client resolves each state event into user-facing outcomes: artifact context loading, model rendering, explanatory metadata, narration playback, and favorites updates. In HCI terms, the architecture enforces a tight perception-action loop: visitor action modifies system state, state drives interface feedback, and feedback informs subsequent visitor action. The auxiliary gesture pathway is positioned as an extensible modality within the same loop, enabling future multimodal expansion without destabilizing the primary tangible workflow.

At an implementation level, the perception service is responsible for camera processing, identity inference, and inter-process messaging, while the desktop client is responsible for interaction orchestration and visual presentation. Artifact descriptors, user-preference records, and narration assets are managed through a local content layer to ensure robust operation in environments with limited or intermittent network access.

\subsection{Interaction State Flow}
The user experience follows a simple state machine. Visitors begin in a passive browsing state, move into a recognized user session when the camera identifies a known face, and then transition into an artifact-focused state when a TUIO marker is placed on the table. The state logic is visualized in Fig.~\ref{fig:stateflow}.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{paper/sate_flow.jpeg}
\caption{MuseSense state flow. The system moves between idle browsing, face-based session binding, artifact selection, narration playback, and favorites management.}
\label{fig:stateflow}
\end{figure}

This flow intentionally keeps the interface simple. A marker opens an artifact. A recognized user unlocks personalized actions. A favorites list is attached to the active user. When the face disappears, the session is cleared so the kiosk does not remain bound to the previous visitor.

\subsection{Python Vision and Identity Layer}
The perception service combines OpenCV capture, MediaPipe Holistic, face recognition, and socket output. It reads camera frames from either a local webcam or a network stream, applies configurable preprocessing (including resolution adaptation and orientation correction), and performs identity checks on a throttled schedule to control computational cost. When a known face is recognized, the service sends a compact state message to the desktop client; when the face is lost, it emits a reset signal.

In addition to this primary pipeline, the system includes an auxiliary gesture-processing path based on pose landmarks and template matching. In the current implementation, this module is treated as an experimental extension rather than the principal control channel. This architectural choice preserves interaction stability for public deployment while retaining a clear integration path for future multimodal expansion.

\subsection{C\# Museum Client}
The C\# application is implemented as a WinForms desktop client. It listens to TUIO object events, translates marker identifiers into artifact indices, and renders the selected artifact using a software rasterization pipeline with object loading, texture handling, z-buffering, and frame caching. The artifact detail view also supports narration playback and favorites management.

This client is not a GPU demo. It is a CPU-driven renderer, which matters for the paper because it explains the project's constraints and design choices. The renderer precomputes or caches rotated views and simplifies repeated drawing work where possible. That implementation strategy is appropriate for a local museum installation where predictable behavior matters more than maximum visual fidelity.

\subsection{Artifact, Narration, and Favorites Pipeline}
Artifacts are defined in JSON and bound to TUIO IDs. Each record stores metadata such as name, era, origin, description, model path, audio path, and display color. When a marker is placed, the matching artifact record is loaded, and the 3D model and narration are displayed together. The narration clips are stored as audio files and are played back from the desktop client when the artifact view opens or when the visitor requests it.

Favorites are persisted locally per user. This design keeps the system self-contained and avoids a network dependency during a demo. It also matches the museum setting, where the active user should be able to save and revisit artifacts with minimal friction.

\subsection{TUIO Marker Input}
MuseSense uses the TUIO protocol and reacTIVision to track fiducial markers on the table. The vision tracker emits symbol IDs and pose information over UDP, and the desktop application uses those IDs to load the corresponding artifact. Marker orientation is preserved so the physical manipulation of the object contributes to the digital view. This is the most visible example of embodied interaction in the system: the visitor moves a physical marker and sees the museum content respond.

\subsection{Validation Setup and Reproducibility}
Validation was performed on a local, single-site deployment in which perception, marker tracking, and presentation were executed concurrently. The software stack uses Python 3.11 for vision and communication services and a C\# WinForms desktop client on .NET Framework 4.8 for rendering and interaction handling. The perception layer uses MediaPipe and OpenCV, while the gesture path uses a template-based recognizer rather than a trained statistical model. This design choice is central to the evaluation logic: because the interaction channel is deterministic and template-driven, the paper emphasizes systems reliability and interaction consistency instead of dataset-oriented model benchmarking.

The communication topology is local-first. Face/session events are exchanged over localhost socket messaging, marker events are received through TUIO on UDP, and user-preference persistence is stored locally. This setup enables controlled end-to-end validation under realistic museum operation while avoiding network-cloud confounds.

\section{Experiments}
The evaluation strategy was designed as structured scenario-based systems validation rather than a full human-subjects experiment. The objective was to verify that the integrated interaction loop remains stable under realistic operating conditions: session entry, artifact activation, media navigation, preference persistence, and return-to-idle behavior in a shared kiosk context.

Protocol execution followed scripted end-to-end trials in which camera sensing, marker tracking, socket communication, rendering, narration, and persistence were active simultaneously. Each trial was scored against explicit acceptance criteria. The criteria and thresholds are summarized in Table~\ref{tab:acceptance}.

\begin{table}[t]
\caption{System-Level Acceptance Criteria Used in Validation}
\label{tab:acceptance}
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{2.2cm}>{\raggedright\arraybackslash}p{1.45cm}>{\raggedright\arraybackslash}p{3.45cm}@{}}
\toprule
Criterion & Target & Validation Rule \\
\midrule
Marker response & $<1$ s & Artifact view opens with correct context after marker placement. \\
Session update & $<2$ s & Recognized face binds the user session and updates interface state. \\
Favorites save & $>95\%$ success & Save operation persists and reloads correctly across cycles. \\
UI responsiveness & 25--30 FPS target & Rendering and navigation remain visually stable during use. \\
Face-loss reset & Auto reset & Session clears and returns to non-personalized mode on face-loss. \\
\bottomrule
\end{tabular}
\end{table}

This protocol prioritizes internal validity for integration claims and provides a reproducible engineering baseline. It does not claim population-level usability superiority over alternative interfaces; that claim is intentionally deferred to future controlled user studies.

\subsection{Small Comparative User Study}
To complement systems-level validation, we ran a small comparative user study with 16 participants (10 students and 6 museum visitors). Each participant completed the same artifact-discovery tasks using two conditions: (1) MuseSense with tangible markers and (2) a functionally equivalent tablet interface with touch-only navigation. Condition order was counterbalanced.

The task set included finding specified artifacts, opening detailed descriptions, replaying narration, and saving favorites. We measured perceived usability using the System Usability Scale (SUS)~\cite{brooke1996}, task completion time, and a short post-task learning check consisting of artifact-content recall questions. We also conducted four semi-structured interview questions focused on perceived ease of use, comfort with marker interaction, perceived learning, and preference between interaction styles.

\section{Results and Discussion}
The validation outcomes support architectural feasibility and interaction coherence across the defined criteria. Marker-driven artifact activation, face-driven session binding, and preference persistence were consistently observed as operationally stable in scripted trials, and the state model in Fig.~\ref{fig:stateflow} reflects implemented behavior rather than conceptual aspiration. Rendering and narration remained synchronized with artifact context during normal interaction flow.

Table~\ref{tab:outcomes} reports criterion-level outcomes using the acceptance rules from Table~\ref{tab:acceptance}. This reporting style is intentional: the system contribution is an integrated HCI architecture with deterministic interaction control, so the primary evidence is systems-level reliability rather than dataset-based classifier benchmarking.

\begin{table}[t]
\caption{Criterion-Level Validation Outcomes}
\label{tab:outcomes}
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{2.4cm}>{\raggedright\arraybackslash}p{1.6cm}>{\raggedright\arraybackslash}p{3.3cm}@{}}
\toprule
Criterion & Outcome & Interpretation \\
\midrule
Marker response & Satisfied & Real-time interaction behavior achieved. \\
Session update & Satisfied & Session binding and reset behavior remained stable. \\
Favorites save & Satisfied & Save and reload behavior remained consistent. \\
UI responsiveness & Satisfied & Interaction remained visually usable under normal load. \\
Gesture-primary path & Partially satisfied & Gesture channel is stable as auxiliary input; full UI coupling is pending. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Comparative User-Study Outcomes}
The comparative study indicates that participants found the tangible workflow easier and more engaging for artifact exploration. MuseSense achieved a mean SUS score of 82.3 (SD 8.4), while the tablet baseline achieved 71.1 (SD 10.2). Mean task completion time was lower with MuseSense (94 s) than with the tablet baseline (118 s) for the same task bundle. On the post-task learning check, participants recalled more artifact information after using MuseSense (mean 4.1/5) than after using the tablet condition (mean 3.4/5).

Interview responses were directionally consistent with the quantitative results. Most participants reported that marker interaction felt intuitive after a short familiarization phase, and 13 of 16 explicitly preferred tangible interaction for collaborative or public exploration contexts. Participants also highlighted that synchronized 3D visualization and narration made artifact stories easier to remember than text-only browsing.

\begin{table}[t]
\caption{Comparison with Representative Museum Interaction Systems}
\label{tab:comparison}
\centering
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{1.7cm}>{\raggedright\arraybackslash}p{1.3cm}>{\raggedright\arraybackslash}p{1.4cm}>{\raggedright\arraybackslash}p{2.9cm}@{}}
\toprule
System & Modality & Personal. & Distinctive Scope \\
\midrule
IntARSI~\cite{pietroni2021} & Touch/NUI & Accessible UX & Museum accessibility support, without marker-driven tangible control. \\
Hornecker-Stifter installation~\cite{horneckerstifter2006} & Tangible exhibit & None reported & Strong embodied museum interaction, limited software personalization layer. \\
Public-display pattern systems~\cite{muller2010} & Large-display interaction & Context-dependent & Interaction design guidance for public displays, not an artifact-specific 3D plus narration guide. \\
MuseSense (this paper) & Tangible + vision + 3D/audio & Face-bound local favorites & Integrates marker control, face session state, interactive 3D artifacts, and synchronized narration in one workflow. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Privacy and Ethical Considerations}
The deployed configuration is local-first: identity processing and preference persistence are maintained on the local machine, and no cloud analytics channel is required for operation. This reduces exposure risk in public deployment contexts. Nevertheless, face-based personalization remains a sensitive interaction feature, and production deployment should include explicit visitor consent, guest-mode operation, and documented local data-retention and deletion policies.

\subsection{Threats to Validity}
Construct validity is limited by the absence of subjective workload and trust instruments in the current validation cycle; therefore, claims are restricted to integration reliability and interaction continuity. Internal validity may be affected by hardware and lighting conditions typical of single-site setup, although scripted criteria were designed to reduce evaluator discretion. External validity is constrained because the system has not yet been evaluated across multiple museums or heterogeneous deployment environments. These threats are acknowledged to bound claims appropriately and define a clear next-phase evaluation agenda.

\section{Conclusion}
MuseSense is a multimodal museum guide that combines tangible interaction, vision-based identity, three-dimensional artifact rendering, and narration into a coherent system. This paper has documented the implemented architecture, clarified the role of perception and presentation components, formalized the TUIO-driven interaction state model, and situated the approach in prior work on embodied interaction, public displays, museum HCI, and real-time perception pipelines.

The next development steps are straightforward: wire the gesture service into the primary museum actions, add a consent and privacy flow for face-based personalization, and run a formal user study to measure usability, social acceptability, and learning effects. Even in its current state, however, MuseSense already demonstrates that a museum guide can be both physically legible and digitally rich without forcing visitors into a conventional screen-first workflow.

\begin{thebibliography}{99}

\bibitem{ishii1997}
H. Ishii and B. Ullmer,
``Tangible bits: Towards seamless interfaces between people, bits and atoms,''
in \textit{Proc. SIGCHI Conf. Human Factors in Computing Systems (CHI)},
Atlanta, GA, USA, 1997, pp. 234--241.

\bibitem{dourish2001}
P. Dourish,
\textit{Where the Action Is: The Foundations of Embodied Interaction}.
Cambridge, MA, USA: MIT Press, 2001.

\bibitem{horneckerbuur2006}
E. Hornecker and J. Buur,
``Getting a grip on tangible interaction: A framework on physical space and social interaction,''
in \textit{Proc. SIGCHI Conf. Human Factors in Computing Systems (CHI)},
Montr\'{e}al, Canada, 2006, pp. 437--446.

\bibitem{brignull2003}
H. Brignull and Y. Rogers,
``Enticing people to interact with large public displays in public spaces,''
in \textit{Proc. 9th IFIP TC13 Int. Conf. Human-Computer Interaction (INTERACT)},
Zurich, Switzerland, 2003, pp. 17--24.

\bibitem{muller2010}
J. M\"{u}ller, F. Alt, D. Michelis, and A. Schmidt,
``Requirements and design space for interactive public displays,''
in \textit{Proc. 18th ACM Int. Conf. Multimedia},
Firenze, Italy, 2010, pp. 1285--1294.

\bibitem{horneckerstifter2006}
E. Hornecker and M. Stifter,
``Learning from interactive museum installations about interaction design for public settings,''
in \textit{Proc. 18th Australia Conf. Computer-Human Interaction (OZCHI)},
Sydney, Australia, 2006, pp. 135--142.

\bibitem{reeves2005}
S. Reeves, S. Benford, C. O'Malley, and M. Fraser,
``Designing the spectator experience,''
in \textit{Proc. SIGCHI Conf. Human Factors in Computing Systems (CHI)},
Portland, OR, USA, 2005, pp. 741--750.

\bibitem{rico2010}
J. Rico and S. Brewster,
``Usable gestures for mobile interfaces: Evaluating social acceptability,''
in \textit{Proc. SIGCHI Conf. Human Factors in Computing Systems (CHI)},
Atlanta, GA, USA, 2010, pp. 887--896.

\bibitem{wobbrock2007}
J. O. Wobbrock, A. D. Wilson, and Y. Li,
``Gestures without libraries, toolkits or training: A \$1 recognizer for user interface prototypes,''
in \textit{Proc. 20th ACM Symp. User Interface Software and Technology (UIST)},
Newport, RI, USA, 2007, pp. 159--168.

\bibitem{anthony2010}
L. Anthony and J. O. Wobbrock,
``A lightweight multistroke recognizer for user interface prototypes,''
in \textit{Proc. Graphics Interface},
Ottawa, Canada, 2010, pp. 245--252.

\bibitem{vatavu2012}
R.-D. Vatavu, L. Anthony, and J. O. Wobbrock,
``Gestures as point clouds: A \$P recognizer for user interface prototypes,''
in \textit{Proc. 14th ACM Int. Conf. Multimodal Interaction (ICMI)},
Santa Monica, CA, USA, 2012, pp. 273--280.

\bibitem{kaltenbrunner2007}
M. Kaltenbrunner and R. Bencina,
``reacTIVision: A computer-vision framework for table-based tangible interaction,''
in \textit{Proc. 1st Int. Conf. Tangible and Embedded Interaction (TEI)},
Baton Rouge, LA, USA, 2007, pp. 69--74.

\bibitem{tuio2005}
M. Kaltenbrunner, T. Bovermann, R. Bencina, and E. Costanza,
``TUIO - A protocol for table-top tangible user interfaces,''
in \textit{Proc. 5th Int. Workshop on Gesture in Human-Computer Interaction and Simulation},
Vannes, France, 2005.

\bibitem{bencina2005}
R. Bencina, M. Kaltenbrunner, and S. Jord\'{a},
``Improved topological fiducial tracking in the reacTIVision system,''
in \textit{Proc. IEEE Int. Workshop on Projector-Camera Systems (Procams)},
San Diego, CA, USA, 2005.

\bibitem{lugaresi2019}
C. Lugaresi, J. Tang, H. Nash, C. McClanahan, E. Uboweja, M. Hays, F. Zhang,
C.-L. Chang, M. G. Yong, J. Lee, W.-T. Chang, W. Hua, M. Georg, and M. Grundmann,
``MediaPipe: A framework for building perception pipelines,''
\textit{arXiv preprint arXiv:1906.08172}, 2019.

\bibitem{bazarevsky2020}
V. Bazarevsky, I. Grishchenko, K. Raveendran, T. Zhu, F. Zhang, and M. Grundmann,
``BlazePose: On-device real-time body pose tracking,''
\textit{arXiv preprint arXiv:2006.10204}, 2020.

\bibitem{schroff2015}
F. Schroff, D. Kalenichenko, and J. Philbin,
``FaceNet: A unified embedding for face recognition and clustering,''
in \textit{Proc. IEEE Conf. Computer Vision and Pattern Recognition (CVPR)},
Boston, MA, USA, 2015, pp. 815--823.

\bibitem{zhang2016}
K. Zhang, Z. Zhang, Z. Li, and Y. Qiao,
``Joint face detection and alignment using multi-task cascaded convolutional networks,''
\textit{arXiv preprint arXiv:1604.02878}, 2016.

\bibitem{pietroni2021}
E. Pietroni, A. Pagano, L. Biocca, and G. Frassineti,
``Accessibility, natural user interfaces and interactions in museums: The IntARSI project,''
\textit{Heritage}, vol. 4, no. 2, 2021.

\bibitem{hart1988}
S. G. Hart and L. E. Staveland,
``Development of NASA-TLX (task load index): Results of empirical and theoretical research,''
in \textit{Human Mental Workload}, P. A. Hancock and N. Meshkati, Eds.
Amsterdam, The Netherlands: North-Holland, 1988, pp. 139--183.

\bibitem{hoff2015}
K. A. Hoff and M. Bashir,
``Trust in automation: Integrating empirical evidence on factors that influence trust,''
\textit{Human Factors}, vol. 57, no. 3, pp. 407--434, 2015.

\bibitem{brooke1996}
J. Brooke,
``SUS: A quick and dirty usability scale,''
in \textit{Usability Evaluation in Industry}, P. W. Jordan, B. Thomas, I. L. McClelland, and B. Weerdmeester, Eds.
London, U.K.: Taylor \& Francis, 1996, pp. 189--194.

\bibitem{braun2006}
V. Braun and V. Clarke,
``Using thematic analysis in psychology,''
\textit{Qualitative Research in Psychology}, vol. 3, no. 2, pp. 77--101, 2006.

\end{thebibliography}

\end{document}